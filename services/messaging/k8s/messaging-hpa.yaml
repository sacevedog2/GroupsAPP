apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: groupsapp-messaging-hpa
  labels:
    app: groupsapp-messaging
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: groupsapp-messaging
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 65
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 70
